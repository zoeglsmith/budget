import { StyleSheet } from 'react-native';

const bucketsStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#E0F7FA',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 24,
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#006064',
    marginBottom: 12,
  },
  subtitle: {
    fontSize: 16,
    color: '#004D40',
  },
});

export default bucketsStyles;
